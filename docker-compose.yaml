version: '2'

networks:
  app-network:
    driver: bridge

services:
  redis:
    image: 'redis:latest'
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    networks:
      - app-network
  myapplication:
    image: 'flask_app:1.7'
    ports:
      - "9000:8080"
    networks:
      - app-network
